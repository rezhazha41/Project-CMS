@component('layouts/public')

  <!-- Hero Section -->
  <section class="relative bg-gradient-to-br from-primary/5 via-white to-primary/10 pt-32 pb-20 md:pt-40 md:pb-28 overflow-hidden">
    <!-- Background Patterns -->
    <div class="absolute top-0 right-0 w-1/2 h-full bg-primary/5 blur-3xl -z-10 rounded-l-full translate-x-1/3"></div>
    <div class="absolute bottom-0 left-0 w-96 h-96 bg-secondary/5 blur-3xl -z-10 rounded-full -translate-x-1/3 translate-y-1/3"></div>

    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 items-center">
        <!-- Text Content -->
        <div class="space-y-8 animate-fade-in-up">
          @if(settings.hero_badge)
          <div class="inline-flex items-center gap-2 px-4 py-2 bg-white rounded-full shadow-sm border border-gray-100 mb-2">
            <span class="w-2 h-2 rounded-full bg-green-500 animate-pulse"></span>
            <span class="text-sm font-bold text-gray-600 tracking-wide uppercase">{{ settings.hero_badge }}</span>
          </div>
          @end
          
          
          <h1 class="text-5xl md:text-6xl lg:text-7xl font-black text-gray-900 leading-tight tracking-tight">
            {{ settings.hero_title || 'Mewujudkan Generasi Unggul & Berakhlak' }}
          </h1>
          
          <p class="text-xl text-gray-600 max-w-lg leading-relaxed font-medium">
            {{ settings.hero_description || 'SMK Al Munawwarah mempersiapkan siswa dengan keterampilan modern dan karakter kuat untuk masa depan gemilang.' }}
          </p>
          
          <div class="flex flex-col sm:flex-row gap-4 pt-4">
             <a href="#ppdb" class="px-8 py-4 bg-primary hover:bg-primary-dark text-white font-bold rounded-full shadow-xl shadow-primary/30 hover:shadow-2xl hover:shadow-primary/40 hover:-translate-y-1 transition-all duration-300 text-center flex items-center justify-center gap-2">
               Daftar Sekarang
               <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"/></svg>
             </a>
             <a href="#profil" class="px-8 py-4 bg-white hover:bg-gray-50 text-gray-900 font-bold rounded-full shadow-lg shadow-gray-100 hover:shadow-xl border border-gray-200 hover:border-gray-300 hover:-translate-y-1 transition-all duration-300 text-center">
               Profil Sekolah
             </a>
          </div>

          <!-- Quick Stats -->
          <div class="grid grid-cols-3 gap-6 pt-8 border-t border-gray-200">
            <div>
              <p class="text-3xl font-black text-gray-900">{{ settings.stat1_value || '1.5k+' }}</p>
              <p class="text-sm text-gray-500 font-medium">{{ settings.stat1_label || 'Siswa Aktif' }}</p>
            </div>
            <div>
              <p class="text-3xl font-black text-gray-900">{{ settings.stat2_value || '100+' }}</p>
              <p class="text-sm text-gray-500 font-medium">{{ settings.stat2_label || 'Guru Terbaik' }}</p>
            </div>
            <div>
              <p class="text-3xl font-black text-gray-900">{{ settings.stat3_value || 'A' }}</p>
              <p class="text-sm text-gray-500 font-medium">{{ settings.stat3_label || 'Akreditasi' }}</p>
            </div>
          </div>
        </div>

        <!-- Hero Image Composition -->
        <div class="relative animate-float lg:h-[600px] flex items-center justify-center">
           <div class="relative z-10 w-full max-w-lg">
             <!-- Main Image Card -->
             <div class="absolute inset-0 bg-primary rounded-[2.5rem] rotate-6 opacity-10"></div>
             <img src="{{ settings.hero_image || 'https://images.unsplash.com/photo-1523050854058-8df90110c9f1?q=80&w=1000&auto=format&fit=crop' }}" class="relative rounded-[2.5rem] shadow-2xl w-full object-cover h-[500px] border-4 border-white" alt="School Activity">
             
             <!-- Floating Badge 1 -->
             <div class="absolute -left-8 top-20 bg-white p-4 rounded-2xl shadow-xl border border-gray-50 flex items-center gap-3 animate-bounce-slow">
               <div class="w-12 h-12 bg-green-100 rounded-full flex items-center justify-center text-2xl text-green-600">🏆</div>
               <div>
                 <p class="font-bold text-gray-900">Juara 1 LKS</p>
                 <p class="text-xs text-gray-500">Tingkat Provinsi 2025</p>
               </div>
             </div>

             <!-- Floating Badge 2 -->
             <div class="absolute -right-8 bottom-32 bg-white p-4 rounded-2xl shadow-xl border border-gray-50 flex items-center gap-3 animate-bounce-slow" style="animation-delay: 1.5s;">
               <div class="w-12 h-12 bg-blue-100 rounded-full flex items-center justify-center text-2xl text-blue-600">💻</div>
               <div>
                 <p class="font-bold text-gray-900">Lab Modern</p>
                 <p class="text-xs text-gray-500">Fasilitas Lengkap</p>
               </div>
             </div>
           </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Fokus Keahlian / Majors -->
  <section class="py-24 bg-gray-50 relative overflow-hidden" id="profil">
    <!-- Decorative blobs -->
    <div class="absolute top-0 left-0 w-64 h-64 bg-primary/5 rounded-full blur-3xl -translate-x-1/2 -translate-y-1/2"></div>
    <div class="absolute bottom-0 right-0 w-64 h-64 bg-secondary/5 rounded-full blur-3xl translate-x-1/2 translate-y-1/2"></div>
    
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
      <div class="text-center max-w-3xl mx-auto mb-16">
        <span class="inline-block py-1 px-3 rounded-full bg-primary/10 text-primary font-bold tracking-wider uppercase text-xs mb-4">Program Keahlian</span>
        <h2 class="text-3xl md:text-4xl lg:text-5xl font-black text-gray-900 leading-tight">Pilih Masa Depanmu</h2>
        <p class="mt-6 text-lg text-gray-600 leading-relaxed">Kami menyediakan berbagai kompetensi keahlian unggulan yang relevan dengan kebutuhan industri 4.0 saat ini.</p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
        @each(dept in departments)
        <a href="/pages/{{ dept.slug }}" class="group bg-white p-8 rounded-[2rem] hover:bg-{{ dept.color || 'green' }}-500 transition-all duration-500 hover:shadow-2xl hover:shadow-{{ dept.color || 'green' }}-500/20 hover:-translate-y-2 border border-gray-100 hover:border-transparent relative overflow-hidden flex flex-col h-full">
          <div class="absolute top-0 right-0 p-8 opacity-5 group-hover:opacity-10 transition-opacity">
             <span class="text-9xl font-black">{{ dept.icon || '🎓' }}</span>
          </div>
          <div class="w-16 h-16 bg-{{ dept.color || 'green' }}-50 rounded-2xl flex items-center justify-center text-3xl mb-8 group-hover:bg-white/20 group-hover:text-white transition-colors shadow-inner">
             {{ dept.icon || '🎓' }}
          </div>
          <h3 class="text-xl font-bold text-gray-900 mb-4 group-hover:text-white transition-colors">{{ dept.name }}</h3>
          <p class="text-gray-500 text-sm group-hover:text-white/90 leading-relaxed mb-8 flex-1 transition-colors">{{ dept.description || 'Program keahlian unggulan.' }}</p>
          <span class="inline-flex items-center gap-2 text-{{ dept.color || 'green' }}-600 font-bold text-sm group-hover:text-white mt-auto">Pelajari Lebih Lanjut <svg class="w-4 h-4 group-hover:translate-x-1 transition-transform" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/></svg></span>
        </a>
        @else
        <div class="col-span-full text-center py-12">
            <p class="text-gray-500">Belum ada data jurusan.</p>
        </div>
        @end
      </div>
    </div>
  </section>

  <!-- News & Agenda Split -->
  <section class="py-24 bg-white" id="agenda">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 lg:grid-cols-12 gap-12 items-start">
        
        <!-- Latest News (Width 8/12) -->
        <div class="lg:col-span-8 space-y-10">
           <div class="flex flex-col sm:flex-row justify-between items-start sm:items-end gap-4 border-b border-gray-100 pb-4">
              <div>
                 <span class="text-primary font-bold tracking-wider uppercase text-xs">Berita Terkini</span>
                 <h2 class="text-3xl font-black text-gray-900 mt-2">Kabar Sekolah</h2>
              </div>
              <a href="/blog" class="px-6 py-2 bg-gray-50 hover:bg-gray-100 text-gray-900 font-bold rounded-full transition text-sm flex items-center gap-2">
                  Lihat Semua Berita <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/></svg>
              </a>
           </div>

           <div class="grid gap-8">
             @each(post in posts)
             <article class="bg-white group cursor-pointer" onclick="window.location='/blog/{{ post.slug }}'">
                <div class="flex flex-col md:flex-row gap-6 items-start">
                    <div class="w-full md:w-64 h-48 rounded-2xl overflow-hidden relative shadow-sm">
                       <img src="{{ post.featuredImage || 'https://via.placeholder.com/400x300?text=News' }}" class="w-full h-full object-cover group-hover:scale-110 transition duration-700">
                       <div class="absolute inset-0 bg-black/0 group-hover:bg-black/10 transition"></div>
                    </div>
                    <div class="flex-1 py-1">
                       <div class="flex items-center gap-3 text-xs text-gray-500 mb-3 font-medium uppercase tracking-wide">
                          <span class="text-primary">{{ post.category?.name || 'Umum' }}</span>
                          <span class="w-1 h-1 bg-gray-300 rounded-full"></span>
                          <span>{{ post.createdAt.toFormat('dd MMMM yyyy') }}</span>
                       </div>
                       <h3 class="text-2xl font-bold text-gray-900 mb-3 group-hover:text-primary transition leading-tight">{{ post.title }}</h3>
                       <p class="text-gray-500 text-sm line-clamp-2 leading-relaxed mb-4">{{ post.excerpt || '' }}</p>
                       <span class="text-gray-900 text-sm font-bold border-b-2 border-primary/20 hover:border-primary transition pb-0.5 inline-block">Baca Selengkapnya</span>
                    </div>
                </div>
             </article>
             @else
              <div class="text-center py-12 bg-gray-50 rounded-3xl border border-dashed border-gray-200">
                  <p class="text-gray-500">Belum ada berita terbaru.</p>
              </div>
             @end
           </div>
        </div>

        <!-- Agenda (Width 4/12) -->
        <div class="lg:col-span-4 sticky top-24">
           <div class="bg-gradient-to-br from-gray-900 to-gray-800 rounded-[2.5rem] p-8 shadow-2xl text-white relative overflow-hidden">
              <div class="absolute top-0 right-0 w-64 h-64 bg-white/5 rounded-full blur-3xl translate-x-1/2 -translate-y-1/2"></div>
               
              <h3 class="text-2xl font-bold mb-8 flex items-center gap-3 relative z-10">
                <span class="w-10 h-10 bg-white/10 rounded-xl flex items-center justify-center text-xl backdrop-blur-sm">🗓</span>
                Agenda Sekolah
              </h3>

              <div class="space-y-8 relative z-10">
                 @each(agenda in agendas)
                 <div class="group cursor-pointer flex gap-4" onclick="window.location='/blog/{{ agenda.slug }}'">
                    <div class="flex-shrink-0 w-14 text-center bg-white/10 rounded-2xl p-2 backdrop-blur-sm group-hover:bg-primary transition border border-white/5 group-hover:border-primary">
                        <span class="block text-xs font-medium text-white/60 group-hover:text-white uppercase">{{ agenda.eventDate ? agenda.eventDate.toFormat('MMM') : agenda.createdAt.toFormat('MMM') }}</span>
                        <span class="block text-xl font-black text-white leading-none mt-1">{{ agenda.eventDate ? agenda.eventDate.day : agenda.createdAt.day }}</span>
                    </div>
                    <div>
                        <h4 class="font-bold text-lg leading-snug group-hover:text-primary transition-colors">{{ agenda.title }}</h4>
                        <p class="text-white/40 text-sm mt-1 line-clamp-2">{{ agenda.excerpt || agenda.content }}</p>
                    </div>
                 </div>
                 @else
                   <p class="text-white/40 italic">Belum ada agenda bulan ini.</p>
                 @end
              </div>
              
              <a href="/agenda" class="mt-8 py-4 bg-white/10 hover:bg-white/20 text-white font-bold rounded-xl transition backdrop-blur-md block text-center w-full relative z-10 border border-white/5">
                Lihat Kalender Akademik
              </a>
           </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Featured Teachers / Biodata Guru -->
  <section class="py-24 bg-white border-t border-gray-100">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="text-center mb-16">
            <span class="text-primary font-bold tracking-wider uppercase text-sm">Tim Pengajar</span>
            <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mt-2">Guru & Staff Inspiratif</h2>
            <p class="mt-4 text-gray-600">Berkenalan dengan para pendidik profesional kami.</p>
          </div>

          <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-8 justify-center">
             @each(teacher in teachers)
             <div class="group text-center">
                <div class="relative w-48 h-48 mx-auto mb-6">
                    <div class="absolute inset-0 bg-primary/20 rounded-full blur-xl group-hover:blur-2xl transition duration-500 opacity-50"></div>
                    <img src="{{ teacher.featuredImage }}" class="w-full h-full object-cover rounded-full relative z-10 border-4 border-white shadow-xl group-hover:scale-105 transition duration-500">
                </div>
                <h3 class="text-xl font-bold text-gray-900 mb-1">{{ teacher.title }}</h3>
                <p class="text-primary font-medium text-sm mb-3">{{ teacher.excerpt }}</p>
                <p class="text-gray-500 text-sm line-clamp-2">{{ teacher.content }}</p>
             </div>
             @else
             <p class="text-gray-500 col-span-3 italic">Belum ada data guru.</p>
             @end
          </div>
      </div>
  </section>

  <!-- Google Maps Embed -->
  @if(settings.map_url)
  <section class="h-96 w-full relative z-0">
     <iframe src="{{ settings.map_url }}" width="100%" height="100%" style="border:0;" allowfullscreen="" loading="lazy" referrerpolicy="no-referrer-when-downgrade"></iframe>
     <div class="absolute top-0 w-full h-24 bg-gradient-to-b from-white to-transparent pointer-events-none"></div>
  </section>
  @end

@end
