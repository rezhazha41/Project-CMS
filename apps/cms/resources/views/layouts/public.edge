<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ settings.site_title || 'CMS Public Site' }}</title>
  
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  
  @if(settings.favicon_url)
    <link rel="icon" type="image/x-icon" href="{{ settings.favicon_url }}">
  @end
  
  @vite(['resources/css/app.css', 'resources/js/app.js'])
  <script src="//unpkg.com/alpinejs" defer></script>
  
  <style>
    :root {
      --color-primary: {{ settings.theme_color || '#059669' }};
      --font-family: {{ settings.font_family || 'Inter' }}, system-ui, sans-serif;
    }
    
    body {
      font-family: var(--font-family);
    }
    
    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .animate-fade-in-up {
      animation: fadeInUp 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
      opacity: 0; /* Start hidden */
    }
    .delay-100 { animation-delay: 0.1s; }
    .delay-200 { animation-delay: 0.2s; }
  </style>
</head>
<body class="antialiased text-gray-800 bg-gray-50 flex flex-col min-h-screen">

  <!-- Announcement Modal -->
  @if(activeAnnouncement)
    <div x-data="{ 
           show: !sessionStorage.getItem('closed_popup_{{ activeAnnouncement.id }}') 
         }" 
         x-show="show" 
         style="display: none;"
         class="fixed inset-0 z-[100] flex items-center justify-center px-4">
         
         <!-- Backdrop with Blur -->
         <div x-show="show" 
              x-transition:enter="transition ease-out duration-300"
              x-transition:enter-start="opacity-0 backdrop-blur-none"
              x-transition:enter-end="opacity-100 backdrop-blur-sm"
              x-transition:leave="transition ease-in duration-200"
              x-transition:leave-start="opacity-100 backdrop-blur-sm"
              x-transition:leave-end="opacity-0 backdrop-blur-none"
              class="absolute inset-0 bg-black/60 backdrop-blur-sm"
              @click="show = false; sessionStorage.setItem('closed_popup_{{ activeAnnouncement.id }}', 'true')">
         </div>

         <!-- Modal Content -->
         <div x-show="show"
              x-transition:enter="transition ease-out duration-300"
              x-transition:enter-start="opacity-0 scale-90 translate-y-4"
              x-transition:enter-end="opacity-100 scale-100 translate-y-0"
              x-transition:leave="transition ease-in duration-200"
              x-transition:leave-start="opacity-100 scale-100 translate-y-0"
              x-transition:leave-end="opacity-0 scale-90 translate-y-4"
              class="relative bg-white rounded-2xl shadow-2xl w-full max-w-lg overflow-hidden transform">
              
              <!-- Header/Image Area (Optional aesthetic) -->
              <div class="h-32 bg-gradient-to-br from-green-600 to-emerald-700 flex items-center justify-center relative overflow-hidden">
                  <div class="absolute inset-0 opacity-20">
                      <svg class="w-full h-full" viewBox="0 0 100 100" preserveAspectRatio="none">
                          <circle cx="0" cy="0" r="40" fill="white" />
                          <circle cx="100" cy="100" r="40" fill="white" />
                      </svg>
                  </div>
                  <svg class="w-16 h-16 text-white/90 animate-bounce" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M11 5.882V19.24a1.76 1.76 0 01-3.417.592l-2.147-6.15M18 13a3 3 0 100-6M5.436 13.683A4.001 4.001 0 017 6h1.832c4.1 0 7.625-1.234 9.168-3v14c-1.543-1.766-5.067-3-9.168-3H7a3.988 3.988 0 01-1.564-.317z"/>
                  </svg>
                  
                  <button @click="show = false; sessionStorage.setItem('closed_popup_{{ activeAnnouncement.id }}', 'true')" class="absolute top-4 right-4 bg-black/20 hover:bg-black/40 text-white rounded-full p-1.5 transition">
                      <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/></svg>
                  </button>
              </div>

              <!-- Body -->
              <div class="p-8 text-center">
                  <h3 class="text-2xl font-bold text-gray-900 mb-2">{{ activeAnnouncement.title }}</h3>
                  <div class="prose prose-sm text-gray-600 mb-6 mx-auto">
                      {{ activeAnnouncement.content }}
                  </div>
                  
                  @if(activeAnnouncement.link)
                    <a href="{{ activeAnnouncement.link }}" class="inline-block w-full bg-green-600 text-white font-bold py-3 rounded-xl hover:bg-green-700 transition shadow-lg shadow-green-600/30">
                        Lihat Informasi →
                    </a>
                  @else
                    <button @click="show = false; sessionStorage.setItem('closed_popup_{{ activeAnnouncement.id }}', 'true')" class="inline-block w-full bg-gray-100 text-gray-700 font-bold py-3 rounded-xl hover:bg-gray-200 transition">
                        Tutup
                    </button>
                  @end
              </div>
         </div>
    </div>
  @end

  <!-- Header -->
  <header class="fixed w-full top-0 z-50 transition-all duration-300" 
          style="background-color: #047857; border-bottom: 1px solid #065f46;"
          x-data="{ scrolled: false, mobileMenuOpen: false }" 
          @scroll.window="scrolled = (window.pageYOffset > 20)">
    
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 h-20 flex items-center justify-between">
      <div class="flex items-center gap-3">
        <!-- Mobile Menu Button -->
        <button @click="mobileMenuOpen = !mobileMenuOpen" class="md:hidden text-white/90 hover:text-white p-2 -ml-2">
            <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path x-show="!mobileMenuOpen" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
                <path x-show="mobileMenuOpen" style="display: none;" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
            </svg>
        </button>

        @if(settings.logo_url)
          <img src="{{ settings.logo_url }}" alt="{{ settings.site_title || 'Logo' }}" class="h-10 object-contain">
        @else
          <div class="w-10 h-10 bg-white rounded-xl transform -rotate-3 flex items-center justify-center text-primary font-bold text-lg">
             <img src="https://ui-avatars.com/api/?name=S+M&background=transparent&color=047857&size=64" class="w-6 h-6 object-contain" alt="Logo">
          </div>
        @end
        <span class="font-bold text-xl tracking-tight text-white truncate max-w-[200px] md:max-w-none">{{ settings.site_title || 'SMK Al Munawwarah' }}</span>
      </div>
      
      <nav class="hidden md:flex gap-8">
        @each(item in globalMenu)
            @if(item.children && item.children.length > 0)
                <div class="relative group">
                    <button class="text-sm font-medium text-white/90 hover:text-white transition flex items-center gap-1">
                        {{ item.label }}
                        <svg class="w-3 h-3 text-white/70 group-hover:text-white transition" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/></svg>
                    </button>
                    <div class="absolute left-0 top-full pt-4 w-56 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-300 transform translate-y-2 group-hover:translate-y-0">
                        <div class="bg-white rounded-xl shadow-xl border border-gray-100 overflow-hidden py-1">
                            @each(child in item.children)
                                <a href="{{ child.url }}" class="block px-4 py-3 text-sm text-gray-700 hover:bg-gray-50 hover:text-primary border-b border-gray-50 last:border-0">{{ child.label }}</a>
                            @end
                        </div>
                    </div>
                </div>
            @else
                <a href="{{ item.url }}" class="text-sm font-medium text-white/90 hover:text-white transition hover:underline decoration-2 underline-offset-4">{{ item.label }}</a>
            @end
        @end
        
        @if(!globalMenu || globalMenu.length === 0)
             <a href="/" class="text-sm font-medium text-white/90 hover:text-white">Home</a>
             <a href="/login" class="text-sm font-medium text-white/90 hover:text-white">Login</a>
        @end
      </nav>

      <div class="flex items-center gap-4">
        <button class="p-2 text-white/80 hover:text-white bg-white/10 hover:bg-white/20 rounded-full transition-all">
           <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/></svg>
        </button>
      </div>
    </div>

    <!-- Mobile Menu Overlay -->
    <div x-show="mobileMenuOpen" 
         x-transition:enter="transition ease-out duration-200"
         x-transition:enter-start="opacity-0 -translate-y-2"
         x-transition:enter-end="opacity-100 translate-y-0"
         x-transition:leave="transition ease-in duration-150"
         x-transition:leave-start="opacity-100 translate-y-0"
         x-transition:leave-end="opacity-0 -translate-y-2"
         class="md:hidden absolute top-20 inset-x-0 bg-white shadow-xl border-t border-gray-100 overflow-y-auto max-h-[calc(100vh-80px)]"
         style="display: none;">
         
         <div class="flex flex-col space-y-4">
            @each(item in globalMenu)
                @if(item.children && item.children.length > 0)
                    <div x-data="{ expanded: false }" class="border-b border-gray-50 pb-2">
                        <button @click="expanded = !expanded" class="flex justify-between items-center w-full text-lg font-bold text-gray-900 py-2">
                            {{ item.label }}
                            <svg class="w-5 h-5 text-gray-400 transition-transform" :class="expanded ? 'rotate-180' : ''" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/></svg>
                        </button>
                        <div x-show="expanded" class="pl-4 space-y-3 mt-1 mb-2 bg-gray-50 rounded-lg p-3">
                            @each(child in item.children)
                                <a href="{{ child.url }}" class="block text-gray-600 hover:text-primary font-medium">{{ child.label }}</a>
                            @end
                        </div>
                    </div>
                @else
                   <a href="{{ item.url }}" class="text-lg font-bold text-gray-900 hover:text-primary py-2 border-b border-gray-50">{{ item.label }}</a>
                @end
            @end

            <!-- Login for Mobile -->
             <a href="/login" class="text-lg font-bold text-primary py-2 mt-4 flex items-center gap-2">
                <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 16l-4-4m0 0l4-4m-4 4h14m-5 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h7a3 3 0 013 3v1"/></svg>
                Login Admin
             </a>
         </div>
    </div>
  </header>

  <!-- Main Content -->
  <main class="flex-grow animate-fade-in-up">
    {{{ await $slots.main() }}}
  </main>

  <!-- Footer -->
  <footer class="pt-16 pb-8 text-white relative overflow-hidden" 
          style="background-color: #047857;">
     <div class="absolute inset-0 bg-gradient-to-t from-black/10 to-transparent pointer-events-none"></div>

    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
      <div class="grid grid-cols-1 md:grid-cols-4 gap-12 mb-12">
        <div class="col-span-1 md:col-span-1">
          <div class="flex items-center gap-3 mb-6">
            @if(settings.logo_url)
              <img src="{{ settings.logo_url }}" alt="Logo Footer" class="w-10 h-10 object-contain hover:scale-110 transition-transform duration-300">
            @else
              <div class="w-8 h-8 bg-primary rounded flex items-center justify-center transform -rotate-6 shadow-lg shadow-primary/50">
                   <span class="font-bold text-white text-xs">SMK</span>
              </div>
            @end
            <span class="font-bold text-lg text-white">{{ settings.site_title || 'SMK Al Munawwarah' }}</span>
          </div>
          <p class="text-gray-400 text-sm leading-relaxed mb-6">
            {{ settings.site_description || 'Mewujudkan Generasi Unggul dan Berakhlak Mulia.' }}
          </p>
        </div>
        
        <div>
          <h4 class="font-bold text-white mb-6 text-sm uppercase tracking-wider">Navigasi</h4>
          <ul class="space-y-4 text-sm text-gray-400">
            <li><a href="/" class="hover:text-primary transition">Beranda</a></li>
            <li><a href="#profil" class="hover:text-primary transition">Profil Sekolah</a></li>
            <li><a href="/blog" class="hover:text-primary transition">Berita Terkini</a></li>
            <li><a href="#agenda" class="hover:text-primary transition">Agenda</a></li>
          </ul>
        </div>

        <div>
           <h4 class="font-bold text-white mb-6 text-sm uppercase tracking-wider">Kontak Kami</h4>
           <ul class="space-y-4 text-sm text-gray-400">
            <li class="flex items-center gap-3">
              <svg class="w-4 h-4 text-primary" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/></svg>
              {{ settings.email || 'info@smkalmunawwarah.sch.id' }}
            </li>
            <li class="flex items-center gap-3">
              <svg class="w-4 h-4 text-primary" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/></svg>
              {{ settings.phone || '(021) 1234-5678' }}
            </li>
             <li class="flex gap-3">
              <svg class="w-4 h-4 text-primary flex-shrink-0 mt-1" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/></svg>
              {{ settings.address || 'Jl. Pendidikan No. 1, Jakarta' }}
            </li>
           </ul>
        </div>

        <div>
          <h4 class="font-bold text-white mb-6 text-sm uppercase tracking-wider">Sosial Media</h4>
          <div class="flex gap-4">
            @if(settings.social_facebook)
            <a href="{{ settings.social_facebook }}" target="_blank" rel="noopener noreferrer" class="w-10 h-10 bg-gray-800 rounded-full flex items-center justify-center text-gray-400 hover:bg-blue-600 hover:text-white transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
               <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24"><path d="M18 2h-3a5 5 0 00-5 5v3H7v4h3v8h4v-8h3l1-4h-4V7a1 1 0 011-1h3z"/></svg>
            </a>
            @end
            @if(settings.social_instagram)
            <a href="{{ settings.social_instagram }}" target="_blank" rel="noopener noreferrer" class="w-10 h-10 bg-gray-800 rounded-full flex items-center justify-center text-gray-400 hover:bg-pink-600 hover:text-white transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
               <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24"><path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163c-3.259 0-3.667.014-4.947.072-4.358.2-6.78 2.618-6.98 6.98-.059 1.281-.073 1.689-.073 4.948 0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98-1.281-.059-1.69-.073-4.949-.073zm0 5.838c-3.403 0-6.162 2.759-6.162 6.162s2.759 6.163 6.162 6.163 6.162-2.759 6.162-6.163c0-3.403-2.759-6.162-6.162-6.162zm0 10.162c-2.209 0-4-1.79-4-4 0-2.209 1.791-4 4-4s4 1.791 4 4c0 2.21-1.791 4-4 4zm6.406-11.845c-.796 0-1.441.645-1.441 1.44s.645 1.44 1.441 1.44c.795 0 1.439-.645 1.439-1.44s-.644-1.44-1.439-1.44z"/></svg>
            </a>
            @end
             @if(settings.social_youtube)
             <a href="{{ settings.social_youtube }}" target="_blank" rel="noopener noreferrer" class="w-10 h-10 bg-gray-800 rounded-full flex items-center justify-center text-gray-400 hover:bg-red-600 hover:text-white transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg">
               <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24"><path d="M19.615 3.184c-3.604-.246-11.631-.245-15.23 0-3.897.266-4.356 2.62-4.385 8.816.029 6.185.484 8.549 4.385 8.816 3.6.245 11.626.246 15.23 0 3.897-.266 4.356-2.62 4.385-8.816-.029-6.185-.484-8.549-4.385-8.816zm-10.615 12.816v-8l8 3.993-8 4.007z"/></svg>
            </a>
            @end
          </div>
        </div>
      </div>
      
      <div class="border-t border-gray-800 pt-8 flex flex-col md:flex-row justify-between items-center gap-4">
        <p class="text-gray-500 text-sm">© 2026 {{ settings.site_title || 'SMK Al Munawwarah' }}. All rights reserved.</p>
        <div class="flex gap-6 text-sm text-gray-500">
           <!-- Removed Privacy & Terms as requested -->
           <span>v1.0.0</span>
        </div>
      </div>
    </div>
  </footer>
</body>
</html>
